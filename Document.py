
class Document:

    def __init__(self, title, filepath, datasource, datasourceType, url, date):
        self.title = title
        self.filepath = filepath
        self.datasource = datasource
        self.datasourceType = datasourceType
        self.url = url
        self.date = date
